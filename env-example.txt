# EJEMPLO DE ARCHIVO .env PARA SENDGRID
# Copia este archivo como .env y completa con tus datos reales

# SendGrid Configuration
SENDGRID_API_KEY=SG.tu-api-key-de-sendgrid-aqui
SENDGRID_FROM_EMAIL=noreply@agmtrading.com
SENDGRID_FROM_NAME=AGM Trading

# Server Configuration
PORT=3001

# 🌐 CONFIGURACIÓN DE PRODUCCIÓN - MT5 API
# ⚠️ SOLO HTTPS EN PRODUCCIÓN

# URL base de la API MT5 (OBLIGATORIO HTTPS) 
VITE_MT5_API_URL=https://your-secure-vps.com

# Configuración de entorno
VITE_ENVIRONMENT=production
VITE_API_VERSION=v1

# Firebase Configuration (Mantener configuración existente)
VITE_FIREBASE_API_KEY=your_firebase_api_key
VITE_FIREBASE_AUTH_DOMAIN=your_firebase_auth_domain
VITE_FIREBASE_PROJECT_ID=your_firebase_project_id
VITE_FIREBASE_STORAGE_BUCKET=your_firebase_storage_bucket
VITE_FIREBASE_MESSAGING_SENDER_ID=your_firebase_messaging_sender_id
VITE_FIREBASE_APP_ID=your_firebase_app_id
VITE_FIREBASE_MEASUREMENT_ID=your_firebase_measurement_id

# Configuración de timeouts y reintentos
VITE_API_TIMEOUT=30000
VITE_API_RETRIES=3

# Debug mode (solo para desarrollo)
VITE_DEBUG_MODE=false

# INSTRUCCIONES:
# 1. Copia este archivo como .env (sin .txt)
# 2. Obtén tu API Key de SendGrid (Settings > API Keys)
# 3. Configura tu email verificado en SendGrid
# 4. Ejecuta: npm install && npm start 